spring:
  kafka:
    bootstrap-servers: localhost:9094

aws:
  dynamodb:
    endpoint: http://localhost:8000
    region: us-east-1
  credentials:
    access-key-id: local
    secret-access-key: local

incident:
  llm:
    provider: gemini
  notify:
    provider: slack
  anomaly:
    detector: ewma
